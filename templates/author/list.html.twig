{% set totalBooks = 0 %}
{% if authors is defined %}
<table border=1>
<tr>
        <th></th>
        <th>ID</th>
        <th>Image</th>
        <th>Nom de lauteur</th>
        <th>Email</th>
        <th>Nombre de livres</th>
    </tr>
{% for author in authors %}
<tr>
<td><a href="{{path('details',{id:author.id})}}">Détails</a> </td>
<td>{{ author.id }}</td>
<td><img src="{{ author.picture }}" width="80px"></td>
<td>{{ author.username |upper }}</td>
<td>{{ author.email }}</td>
<td>{{ author.nb_books }}</td>
</tr>
{% set totalBooks = totalBooks + author.nb_books %}
{% endfor %}
</table>
{% else %}
<h2>tableau est vide </h2>
{% endif %}  


<p>Nombre d'auteurs : {{ authors|length }}</p>
  

<p>Nombre total de livres de tous les auteurs : {{ totalBooks }}</p>


